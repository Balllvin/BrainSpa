# Vendor snapshots

Reference copies used by the unified UI (not full legacy repos).

- `brain-spa/` — telegram config template, README excerpt
- `brain-dataset-forge/` — transcript_lab + chess_assistant examples
- `recovered-brain-spa/` — data-spa `frontend/src` snapshot

Full originals live in your external `retired` folder.
